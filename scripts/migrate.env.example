# Настройки одноразового переноса острова. Скопируй этот файл в «migrate.env»
# (без .example), заполни значения и запусти:  node scripts/migrate-island.mjs
# Файл migrate.env в git не попадает (в нём пароли).

# --- СТАРЫЙ сервер (Supabase.com, откуда переносим) ---
OLD_URL=https://fbmzlorrbobtpytcwpcf.supabase.co
OLD_ANON_KEY=sb_publishable_kaeB65VC4AOCOehJLAWJIg_zzRQ0Yon
OLD_LOGIN=твой-логин-на-старом
OLD_PASSWORD=твой-пароль-на-старом

# Код острова, который переносим (6 символов, как в приложении)
WORLD_CODE=ABC123

# --- НОВЫЙ сервер (свой Supabase на Timeweb, куда переносим) ---
NEW_URL=https://supabase.example.ru
NEW_ANON_KEY=anon-ключ-нового-сервера
NEW_LOGIN=логин-нового-аккаунта
NEW_PASSWORD=пароль-нового-аккаунта

# Принудительно добавить, даже если на новом сервере уже есть воспоминания
# этого острова (иначе скрипт остановится, чтобы не создать дубли). 1 = да.
# FORCE=1
